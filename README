flask db migrate -m "Add is_superadmin to User"
flask db migrate -m "Add coach_id to User"
flask db upgrade


flask db migrate
flask db upgrade
sudo -u postgres psql

flask db init

psql -U myapp_user -d myapp_dev

psql -U myapp_user -h localhost -d myapp_dev
